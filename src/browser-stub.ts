/**
 * Browser stub for @nexifi/mdx-blog
 *
 * This package is server-only / build-time only.
 * If you see this error, you are importing @nexifi/mdx-blog in client-side code.
 *
 * Solutions:
 * - Move the import to a Server Component (no 'use client' directive)
 * - Move the import to an API route, loader, or getServerSideProps/getStaticProps
 * - Use dynamic import with { ssr: true } if you need it in a Next.js page
 */
throw new Error(
  "[@nexifi/mdx-blog] This package is server-only and cannot be imported in browser/client code. " +
    "Import it only in Server Components, API routes, loaders, getServerSideProps, or build scripts."
);
